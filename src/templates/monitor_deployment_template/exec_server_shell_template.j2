#!/bin/bash

TARGET_PATH="/root/atom_service_dev/server_compiling/db/server/{{server_name}}/bin"

cd "$TARGET_PATH" || {
    echo "exec not found: $TARGET_PATH"
    exit 1
}
./{{server_name}}_sync_server